<h1>Actor #<%= @the_actor.id %> details</h1>

<a href="/actors">Go back</a>

<hr>

<dl>
  <dt>Name</dt>
  <dd><%= @the_actor.name %></dd>

  <dt>Dob</dt>
  <dd><%= @the_actor.dob %></dd>

  <dt>Bio</dt>
  <dd><%= @the_actor.bio %></dd>

  <dt>Image</dt>
  <dd><img src="<%= @the_actor.image %>"></dd>

  <dt>Created at</dt>
  <dd><%= @the_actor.created_at %></dd>

  <dt>Updated at</dt>
  <dd><%= @the_actor.updated_at %></dd>
</dl>


<h2>Filmagraphy</h2>
<table border="1">
  <tr>
    <th>Title</th>
    <th>Year</th>
    <th>Director</th>
    <th>Character name</th>
    <th></th>
  </tr>

  <% matching_characters = Character.where({:actor_id => @the_director.id})
  matching_character.each do |a_character| %>

  <% character_movie = Movie.where({:id => a_character.id}) %>

  <tr>
    <td><%= a_character.id %></td>
    <td>
      <imag src="<%= a_movie.image %>" width="50">
    </td>
    <td><%= a_movie.title %></td>
    <td><%= a_movie.year %></td>
    <td><%= @the_director.name %></td>
    <td>
      <a href="/movies/<%= a_movie.id %>">Show details</a>
    </td>
  </tr>

  <% end %>

</table>
